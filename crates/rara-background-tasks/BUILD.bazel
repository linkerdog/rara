load("@crate_index//:defs.bzl", "aliases", "all_crate_deps")
load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

package(default_visibility = ["//visibility:public"])

rust_library(
    name = "rara_background_tasks",
    srcs = glob(["src/**/*.rs"]),
    aliases = aliases(),
    crate_name = "rara_background_tasks",
    edition = "2024",
    deps = all_crate_deps(normal = True) + [
        "//crates/rara-tools:rara_tools",
    ],
    proc_macro_deps = all_crate_deps(proc_macro = True) + ["//crates/tool-macros:rara_tool_macros"],
)

rust_test(
    name = "rara_background_tasks_tests",
    crate = ":rara_background_tasks",
    aliases = aliases(
        normal_dev = True,
        proc_macro_dev = True,
    ),
    edition = "2024",
    deps = all_crate_deps(normal_dev = True) + [
        "//crates/rara-tools:rara_tools",
    ],
    proc_macro_deps = all_crate_deps(proc_macro_dev = True) + ["//crates/tool-macros:rara_tool_macros"],
)
