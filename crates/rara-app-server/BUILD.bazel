load("@crate_index//:defs.bzl", "aliases", "all_crate_deps")
load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

package(default_visibility = ["//visibility:public"])

rust_library(
    name = "rara_app_server",
    srcs = glob(["src/**/*.rs"]),
    aliases = aliases(),
    crate_name = "rara_app_server",
    edition = "2024",
    deps = all_crate_deps(normal = True) + ["//crates/instructions:rara_instructions"],
    proc_macro_deps = all_crate_deps(proc_macro = True),
)

rust_test(
    name = "rara_app_server_tests",
    crate = ":rara_app_server",
    aliases = aliases(
        normal_dev = True,
        proc_macro_dev = True,
    ),
    edition = "2024",
    deps = all_crate_deps(normal_dev = True) + ["//crates/instructions:rara_instructions"],
    proc_macro_deps = all_crate_deps(proc_macro_dev = True),
)
